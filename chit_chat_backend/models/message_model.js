

class MessageModel {
    constructor(mssgId, conversationId, messageContent, timestamp) {
        this.mssgId = mssgId;
        this.conversationId = conversationId;
        this.messageContent = messageContent;
        this.timestamp = timestamp;
    }
}